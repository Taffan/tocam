# 📁 Дерево приложения — Фото Отчёты (PWA)

## Структура проекта

```
Фото Отчёты PWA (taffan.github.io/tocam/)
│
├── 🗂️ Файлы проекта
│   ├── 📄 index.html          (309 строк) — HTML-интерфейс, 6 страниц + модалка
│   ├── 📄 styles.css          (1672 строки) — CSS-стили (Apple-style, адаптив)
│   ├── 📄 app.js              (1214 строк) — вся логика
│   ├── 📄 data.js             (292 строки) — TYPE_CONFIGS, PHOTO_TYPES
│   ├── 📄 sw.js               (48 строк)  — Service Worker (v7, cache-first)
│   ├── 📄 manifest.json       — PWA-манифест
│   └── 📁 lib/
│       ├── 📦 jszip.min.js    (97 KB)   — создание ZIP-архивов
│       ├── 📦 zxing.min.js    (292 KB)  — сканер штрих-кодов (ZXing fallback)
│       └── 📦 xlsx.min.js     (881 KB)  — создание XLSX-отчётов
│
├── 🗂️ Страницы (SPA)
│   │   Страница       ID                 Кнопка/триггер
│   ├── 🏠 Главная     #page-home         btn-new-report / btn-install-home
│   ├── ⚙️ Настройка   #page-config       config-form submit
│   ├── 📝 Данные      #page-data         report-form submit
│   ├── ✅ Чек-лист    #page-checklist    клик по секции
│   ├── 📸 Фото        #page-photos       camera-input / gallery-input
│   └── 🎉 Готово      #page-complete     btn-share / btn-download
│
├── 🗂️ Модальные окна
│   ├── 📷 КЕ-камера   #ke-camera-modal   openKECamera(typeId)
│   ├── 📱 Установка   #install-prompt    beforeinstallprompt
│   └── 🍎 iOS инфо    #ios-install       инструкция для iOS
│
├── 📐 Навигация
│   │   pageHistory = ['home', ...]
│   │   showPage(name) → скрыть все .page, показать #page-{name}
│   │   goBack() → pageHistory.pop()
│   │   header-back — универсальная кнопка назад
│
├── 🧠 app.js — основные функции
│
│   ├── ⚡ Инициализация (IIFE, строка 1)
│   │   ├── SW-kill:    unregister() + reload если был старый SW
│   │   ├── cache-kill: caches.delete() всех кэшей
│   │   ├── initDB()    → IndexedDB PhotoReportsDB_v6
│   │   ├── setupEventListeners()
│   │   ├── setDefaultDate()
│   │   └── setupInstallPrompt() → PWA install + карточка на главной
│   │
│   ├── 📋 Страница "Главная" (#page-home)
│   │   ├── loadDrafts()          → IndexedDB.getAll()
│   │   ├── renderDrafts()        → карточки черновиков с удалением
│   │   ├── deleteReport(id)      → IndexedDB.delete()
│   │   ├── install-card          → кнопка установки PWA
│   │   └── click draft → showChecklist()
│   │
│   ├── ⚙️ Страница "Настройка" (#page-config)
│   │   ├── selectType(type)      → выбор МК/ММ/МА
│   │   ├── renderEquipmentConfig(type) → динамические поля оборудования
│   │   ├── hasEquipment()        → проверка наличия оборудования
│   │   ├── updateConfigButton()  → блокировка кнопки "Продолжить"
│   │   └── submit → showPage('data')
│   │
│   ├── 📝 Страница "Данные" (#page-data)
│   │   ├── renderConfigSummary() → сводка выбранного оборудования
│   │   └── handleFormSubmit()    → создание currentReport
│   │       ├── generateSections(type, counts) → из data.js
│   │       ├── saveReport()      → IndexedDB.put()
│   │       └── showChecklist()
│   │
│   ├── ✅ Страница "Чек-лист" (#page-checklist)
│   │   ├── renderSectionsChecklist() → список секций с прогрессом
│   │   ├── updateProgress()      → прогресс-бар (проценты)
│   │   ├── finishReport()        → status='completed', showComplete()
│   │   └── click section → showPhotoSection()
│   │
│   ├── 📸 Страница "Фото" (#page-photos)
│   │   ├── renderPhotoTypes(section) → типы фото + КЕ с состоянием
│   │   │   └── КЕ-тип → openKECamera(typeId)
│   │   │   └── Фото-тип → camera-input.click()
│   │   ├── renderSectionPhotos(section) → сетка фото + удаление
│   │   ├── handleCameraCapture() → камера → processImage()
│   │   ├── handleGallerySelect() → галерея → processImage()
│   │   ├── processImage(file)    → resize до 1920px → dataUrl
│   │   ├── saveCurrentSection()  → проверка полей → сохранение
│   │   └── scheduleAutoSave()    → автосохранение через 2с
│   │
│   ├── 📷 КЕ-камера (модалка #ke-camera-modal)
│   │   ├── openKECamera(typeId)  → getUserMedia + BarcodeDetector | ZXing
│   │   ├── onKECamDetected(code) → рамка зелёная + вибрация
│   │   ├── onKECamLost()         → рамка красная
│   │   ├── captureKEPhoto()      → фото из video → dataUrl
│   │   │   └── сохраняет фото + код ШК в currentReport
│   │   └── closeKECamera()       → остановка камеры + очистка
│   │
│   ├── 🎉 Страница "Готово" (#page-complete)
│   │   ├── buildZipBlob()        → создание ZIP (JSZip)
│   │   │   ├── XLSX (SheetJS)
│   │   │   ├── report.json
│   │   │   ├── КЕ.txt
│   │   │   └── Фото (КЕ/ + корень)
│   │   ├── doShareOrDownload()   → navigator.share | downloadBlob
│   │   ├── downloadArchive()     → downloadBlob()
│   │   ├── downloadBlob()        → createObjectURL + <a>.click()
│   │   └── showToast(msg)        → тост-уведомление
│   │
│   └── 🗄️ Хранилище
│       ├── IndexedDB: PhotoReportsDB_v6.reports
│       │   └── keyPath: id (string)
│       ├── localStorage: iosInstallShown, installPromptShown, theme
│       └── sessionStorage: sw_killed ('1')
│
├── 🌐 Service Worker (sw.js)
│   ├── CACHE_NAME: 'foto-reports-v7'
│   ├── PRECACHE: [./, index.html, *.css, *.js, manifest.json, lib/*.js]
│   ├── install:  cache.addAll(PRECACHE) + skipWaiting()
│   ├── activate: delete old caches + clients.claim()
│   └── fetch:    cache-first → только если нет в кэше → network → кэш
│
├── 📱 PWA Установка
│   ├── display-mode: standalone
│   ├── Карточка на главной: "Установите приложение"
│   ├── beforeinstallprompt → deferredPrompt
│   ├── appinstalled → скрыть карточку
│   └── iOS: инструкция Share → На экран «Домой»
│
└── 🔄 Жизненный цикл SW
    ├── Первый запуск: SW-kill (unregister + reload) → регистрация v7
    ├── Кэширование: все файлы + lib/* при установке
    ├── Режим: cache-first (офлайн после первого запуска)
    └── Обновление: SW v7 → v8 через reg.update() каждые 60с
```

## Навигация (pageHistory)

```
home → config → data → checklist → photos → checklist → complete → home
                         ↑_____________________________|
```

- `header-back` всегда показывает предыдущую страницу
- `checklist` → клик по секции → `photos`
- `photos` → КЕ-тип → модалка `#ke-camera-modal` (поверх всего)
- `photos` → "Сохранить" → `checklist`
- `checklist` → "Завершить отчёт" → `complete`

## Управление отчётом

```
1. Создание:    config → data → checklist (currentReport.id = generateId)
2. Редактирование: клик по черновику → checklist
3. Фото:        клик по типу → камера / КЕ-модалка
4. Завершение:  finishReport() → showComplete() → buildZipBlob()
5. Экспорт:     doShareOrDownload() | downloadArchive()
6. Хранение:    IndexedDB — автосохранение каждые 2с после изменений
```

## Типы объектов (data.js)

| Тип | Название | Оборудование | Разделитель |
|-----|----------|-------------|-------------|
| mk  | Магнит Косметик | Касса, ТД, ТСД, УКС, МП | (пусто) |
| mm  | Магнит Супермаркет | Кассовая зона, КСО, ТД, ТСД, УКС, Весы, МП | ` # ` |
| ma  | Магнит Аптека | Кассовая зона, ТД, ТСД, УКС | ` # ` |

## Ключевые переменные

| Переменная | Тип | Назначение |
|-----------|-----|-----------|
| `currentReport` | object | Текущий отчёт (IndexedDB) |
| `currentSectionIndex` | number | Индекс текущей секции |
| `selectedPhotoType` | string | ID выбранного типа фото |
| `selectedType` | string | "mk" / "mm" / "ma" |
| `equipmentCounts` | object | { kassa: 2, td: 1, ... } |
| `deferredPrompt` | Event | beforeinstallprompt для установки |
| `cachedBlob` | Blob | Предсобранный ZIP-архив |
| `keCamStream` | MediaStream | Поток камеры КЕ-модалки |
| `keCamDetectedCode` | string | Последний распознанный ШК |
