# 📁 Дерево приложения — Фото Отчёты (PWA)

## Структура проекта

```
Фото Отчёты PWA
│
├── 🗂️ Файлы проекта
│   ├── 📄 index.html          — HTML-интерфейс, 8 страниц + модалка сканера
│   ├── 📄 styles.css         — CSS-стили (Apple-style, адаптив)
│   ├── 📄 app.js             — вся логика приложения
│   ├── 📄 data.js            — TYPE_CONFIGS, PHOTO_TYPES, generateSections()
│   ├── 📄 sw.js              — Service Worker (cache-first)
│   ├── 📄 manifest.json       — PWA-манифест
│   └── 📁 lib/
│       ├── 📦 jszip.min.js    — создание ZIP-архивов
│       ├── 📦 zxing.min.js   — сканер штрих-кодов (fallback)
│       └── 📦 xlsx.min.js    — создание XLSX-отчётов

├── 🗂️ Страницы (SPA)
│   │   Страница       ID                 Кнопка/триггер
│   ├── 🏠 Главная     #page-home         btn-new-report
│   ├── ⚙️ Настройка   #page-config       выбор типа объекта, конфигурация
│   ├── 📝 Данные      #page-data         date, technician, comment
│   ├── ✅ Чек-лист    #page-checklist    клик по секции
│   ├── 📸 Фото        #page-photos       съёмка фото
│   ├── ❓ Справка     #page-help         описание функционала
│   ├── 🎛️ Настройки  #page-settings      качество фото/сканера, тёмная тема
│   └── 🎉 Готово      #page-complete      экспорт отчёта

├── 🗂️ Модальные окна
│   ├── 📷 КЕ-камера   #ke-camera-modal   сканер штрихкодов
│   ├── 🖼️ Превью      #photo-preview-modal превью фото с Ок/Удалить
│   ├── 🖼️ Галерея     #photo-gallery-modal камера + миниатюры
│   ├── 📱 Установка   #install-prompt    beforeinstallprompt
│   └── 🍎 iOS инфо    #ios-install       инструкция для iOS

├── 🗂️ Меню
│   └── Бургер-меню (.header-actions → header-menu)
│       ├── Установить приложение
│       └── Настройки

├── 📐 Навигация
│   │   pageHistory = ['home', ...]
│   │   showPage(name) → скрыть все .page, показать #page-{name}
│   │   goBack() → pageHistory.pop()
│   │   header-back — универсальная кнопка назад

├── 🧠 app.js — основные функции

│   ├── ⚡ Инициализация (IIFE)
│   │   ├── SW-kill: unregister() + reload если был старый SW
│   │   ├── cache-kill: caches.delete() всех кэшей
│   │   ├── initDB() → IndexedDB PhotoReportsDB_v6
│   │   ├── setupEventListeners()
│   │   ├── setDefaultDate()
│   │   └── setupInstallPrompt() → PWA install
│   │
│   ├── 📋 Страница "Главная" (#page-home)
│   │   ├── loadDrafts() → IndexedDB.getAll()
│   │   ├── renderDrafts() → карточки черновиков
│   │   ├── deleteReport(id) → IndexedDB.delete()
│   │   └── click draft → showChecklist()
│   │
│   ├── ⚙️ Страница "Настройка" (#page-config)
│   │   ├── selectType(type) → выбор МК/ММ/МА
│   │   ├── renderEquipmentConfig(type) → динамические поля
│   │   ├── hasEquipment() → проверка наличия оборудования
│   │   └── submit → showPage('data')
│   │
│   ├── ❓ Страница "Справка" (#page-help)
│   │   ├── Кнопка "?" между лого и меню (.header-actions)
│   │   ├── Повторный тап → закрыть (возврат на предыдущую страницу)
│   │   └── Описание: создание отчёта, фото, сканер, галерея, завершение
│   │
│   ├── 🎛️ Страница "Настройки" (#page-settings)
│   │   ├── loadSettings() → localStorage
│   │   ├── saveSetting(key, value) → localStorage.setItem()
│   │   ├── Качество фото: Эконом (1024px) / Среднее (1920px) / Высокое (3264px)
│   │   ├── Качество сканера: Низкое (640×480) / Среднее (1280×720) / Высокое (1920×1080)
│   │   ├── Тёмная тема: .dark класс на &lt;html&gt;
│   │   └── Меню → "Настройки" → showPage('settings')
│   │
│   ├── 📝 Страница "Данные" (#page-data)
│   │   ├── renderConfigSummary() → сводка выбранного оборудования
│   │   └── handleFormSubmit() → создание currentReport
│   │       ├── generateSections(type, counts)
│   │       ├── saveReport() → IndexedDB.put()
│   │       └── showChecklist()
│   │
│   ├── ✅ Страница "Чек-лист" (#page-checklist)
│   │   ├── renderSectionsChecklist() → список секций с прогрессом
│   │   ├── updateProgress() → прогресс-бар (проценты)
│   │   ├── finishReport() → status='completed'
│   │   └── click section → showPhotoSection()
│   │
│   ├── 📸 Страница "Фото" (#page-photos)
│   │   ├── renderPhotoTypes(section)
│   │   │   ├── Фото (нет фото) → camera-input.click()
│   │   │   ├── Фото (есть фото) → openPhotoGallery() — модалка с камерой + галерея
│   │   │   ├── КЕ/СН (нет фото) → openKEScanner()
│   │   │   ├── КЕ/СН (есть фото) → openPhotoPreview() — превью с Ок/Удалить
│   │   │   ├── KE/SN — парные строки (ke-pair), кнопки одной высоты
│   │   │   └── Долгое нажатие (3 сек) на любой тип → gallery-input.click() (выбор из галереи)
│   │   ├── openPhotoGallery() → gallery-modal: камера + миниатюры, тап → превью
│   │   ├── galleryPhotoTap() → preview-modal с Ок/Удалить (с кнопкой назад)
│   │   ├── renderGalleryGrid() → сетка миниатюр с удалением per-item
│   │   ├── openPhotoPreview() / closePreview() — превью для KE/SN
│   │   ├── deletePreviewPhoto() → удаление фото + ререндер
│   │   ├── renderSectionPhotos(section) → сетка фото + удаление
│   │   ├── handleCameraCapture() → камера → processImage()
│   │   ├── processImage(file) → resize (по настройкам: 1024/1920/3264px) → dataUrl
│   │   │   └── если gallery-modal открыт → renderGalleryGrid(), иначе renderSectionPhotos
│   │   └── scheduleAutoSave() → автосохранение через 2с
│   │
│   ├── 📷 КЕ-камера (модалка #ke-camera-modal)
│   │   ├── openKEModal() → getUserMedia + BarcodeDetector / ZXing
│   │   ├── isKECode(code) — 13/4 цифры для KE, любой код для SN
│   │   ├── showBarcodeOverlays() → жёлтые рамки (с кэшем cachedOverlayCodes)
│   │   ├── selectBarcodeCode(code) → панель подтверждения (фильтр по isKECode)
│   │   ├── onScanCodeFound(code) → авто-выбор (ZXing) (фильтр по isKECode)
│   │   ├── updateTrackingUI(found) → зелёная/красная рамка + статус
│   │   ├── pendingScanCode — глобальный код для трекинга
│   │   ├── keCamCapture() → фото из видео + addKECode(pendingScanCode)
│   │   └── closeKEModal() → остановка камеры + очистка pendingScanCode
│   │
│   ├── 🎉 Страница "Готово" (#page-complete)
│   │   ├── preBuildArchive() → buildZipBlob() при загрузке страницы
│   │   ├── buildZipBlob()
│   │   │   ├── XLSX (SheetJS)
│   │   │   ├── report.json
│   │   │   ├── Коды.txt
│   │   │   ├── КЕ/ (фото КЕ)
│   │   │   ├── Серийные номера/ (фото СН)
│   │   │   └── Фото (обычные)
│   │   ├── sendReport() → navigator.share | downloadBlob
│   │   └── showToast(msg) → тост-уведомление
│   │
│   └── 🗄️ Хранилище
│       ├── IndexedDB: PhotoReportsDB_v6.reports
│       ├── localStorage: iosInstallShown, installPromptShown, photoQuality, scannerQuality, darkTheme
│       └── sessionStorage: sw_killed ('1')

├── 🌐 Service Worker (sw.js)
│   ├── CACHE_NAME: 'foto-reports-v7'
│   ├── PRECACHE: все статические файлы
│   ├── install: cache.addAll(PRECACHE)
│   ├── activate: delete old caches
│   └── fetch: cache-first

├── 📱 PWA Установка
│   ├── display-mode: standalone
│   ├── beforeinstallprompt → deferredPrompt
│   └── iOS: инструкция Share → На экран «Домой»

└── 🔄 Жизненный цикл SW
    ├── Первый запуск: SW-kill → регистрация нового
    ├── Кэширование: все файлы при установке
    ├── Режим: cache-first (офлайн после первого запуска)
    └── Обновление: reg.update() каждые 60с
```

## Навигация (pageHistory)

```
home → config → data → checklist → photos → checklist → complete → home
                         ↑_____________________________|
```

- `header-back` всегда показывает предыдущую страницу
- Справка и Настройки — отдельные страницы, не входят в основной флоу
- `checklist` → клик по секции → `photos`
- `photos` → "Сохранить" → `checklist`
- `checklist` → "Завершить отчёт" → `complete`

## Управление отчётом

```
1. Создание:    config → data → checklist (currentReport.id = generateId)
2. Редактирование: клик по черновику → checklist
3. Фото:        клик по типу → камера / КЕ-модалка
4. Завершение:   finishReport() → showComplete() → buildZipBlob()
5. Экспорт:     sendReport() → navigator.share | downloadBlob()
6. Хранение:    IndexedDB — автосохранение каждые 2с
```

## Типы объектов (data.js)

| Тип | Название | Оборудование | Разделитель |
|-----|----------|-------------|-------------|
| mk  | Магнит Косметик | Касса, ТД, ТСД, УКС, МП | (пусто) |
| mm  | Магнит Супермаркет | Кассовая зона, КСО, ТД, ТСД, УКС, Весы, МП, РМД | ` # ` |
| ma  | Магнит Аптека | Кассовая зона, ТД, ТСД, УКС | ` # ` |

## Ключевые переменные

| Переменная | Тип | Назначение |
|-----------|-----|-----------|
| `currentReport` | object | Текущий отчёт (IndexedDB) |
| `currentSectionIndex` | number | Индекс текущей секции |
| `selectedPhotoType` | string | ID выбранного типа фото |
| `selectedType` | string | "mk" / "mm" / "ma" |
| `equipmentCounts` | object | { kassa: 2, td: 1, ... } |
| `deferredPrompt` | Event | beforeinstallprompt |
| `cachedZipBlob` | Blob | Предсобранный ZIP-архив |
| `videoStream` | MediaStream | Поток камеры |
| `scanTimer` | number | Таймер сканирования |
| `scanCooldown` | boolean | Защита от дубликатов |
| `pendingScanCode` | string/null | Выбранный код для трекинга |
| `lastScannedCode` | string | Последний отсканированный код |
| `cachedOverlayCodes` | string | Кэш набора кодов для оверлеев (anti-flicker) |
| `previewPhotoIdx` | number | Индекс фото в превью (-1 если закрыто) |
| `currentGallerySection` | object/null | Текущая секция для галереи |
| `galleryPreviewActive` | boolean | true: превью открыто из галереи, closePreview → вернуть галерею |

## PhotoType флаги

| Флаг | Описание | Действие | Фильтр сканера |
|------|---------|----------|----------------|
| `isKE` | Инвентарный номер | Открывает сканер штрихкодов | Только 13/4 цифры |
| `isSN` | Серийный номер | Открывает сканер штрихкодов | Любой ШК/QR |
| `multi` | Можно несколько фото | До `maxPhotos` штук | — |

## Экспорт архива

```
ZIP-архив/
├── report.json
├── Коды.txt
├── {Формат} {Объект} ЧЛ ТО {Формат} ({Техник}) .xlsx
├── КЕ/
│   └── *.jpg (фото инвентарных номеров)
├── Серийные номера/
│   └── *.jpg (фото серийных номеров)
└── N# *.jpg (обычные фото)
```
